<form action="{{ path('liste_doctor_delete', {'id': rendez_vou.rdvId}) }}" method="post" style="display: inline;">
                    <input type="hidden" name="_method" value="DELETE">
                    {{ csrf_token('delete' ~ rendez_vou.rdvId) }}
                    <button type="submit" class="btn btn-primary btn-sm">Cancel Appointment</button>
                </form>