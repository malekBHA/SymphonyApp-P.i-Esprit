<form method="post" action="{{ path('app_r_d_v_delete', {'id': rendez_vou.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rendez_vou.id) }}">
    <button class="btn" style="color: red ;" >Delete</button>
</form>
