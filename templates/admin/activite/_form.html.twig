<!DOCTYPE html>
<html lang="en">

	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>Vitalize</title>

		{% block css %}
			<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
		{% endblock %}

		<style>
			{% block stylesheet %}

				
				

				.form-container {
					width: 700px;
					padding: 20px;
					border: 1px solid #ccc;
					border-radius: 10px;
					box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
					margin: auto auto 30px; /* Center the form horizontally */
				}

				.form-container label {
					margin-bottom: 5px;
				}

				.form-container .form-control {
					width: 100%;
					box-sizing: border-box;
					margin-bottom: 10px;
				}

				.form-container button {
					width: 100%;
				}
				.label-container {
					display: inline-block;
					margin-right: 20px; /* Adjust as needed for spacing */
				}

				
			{% endblock %}

		</style>
	</head>

	<div class="lower-content">

		<div class="form-container">
			{{ form_start(form, {'method': 'POST', 'attr': {'novalidate': 'novalidate'}}) }}
			<h1>Nouvelle Activité:</h1>
			<div class="mb-3">
				<label for="{{ form.type_activite.vars.id }}">{{ form.type_activite.vars.label }}</label>
				{{ form_row(form.type_activite, {'attr': {'class': 'form-control'}}) }}
			</div>

			<div class="mb-3">
				<label for="{{ form.Description.vars.id }}" class="form-label">{{ form.Description.vars.label }}</label>
				{{ form_row(form.Description, {'attr': {'class': 'form-control' }}) }}
			</div>
			<div class="label-container">
				Dureé
			</div>

			<div class="label-container">
				{{ form_row(form.hours, {'attr': {'class': 'form-control'}}) }}
			</div>

			<div class="label-container">
				{{ form_row(form.minutes, {'attr': {'class': 'form-control'}}) }}
			</div>
			<div class="mb-3">

				{{ form_row(form.idevenement, {'attr': {'class': 'form-control' }}) }}
			</div>
			<div class="mb-3">
				<label for="{{ form.imageAct.vars.id }}" class="form-label">{{ form.imageAct.vars.label }}</label>
				{{ form_row(form.imageAct, {'attr': {'class': 'form-control' }}) }}
			</div>
			<button type="submit" class="btn btn-info">{{ button_label|default('Save') }}</button>
			{{ form_end(form) }}

		</div>
		
</div>
		{% block js %}
			<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
		{% endblock %}
	
</html>
