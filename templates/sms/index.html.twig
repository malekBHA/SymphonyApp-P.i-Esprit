<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CSS-Only Floating Labels</title>
  <style>

    /* #################### ################################ #################### */
    /* #################### RELEVANT CODE STARTS AT LINE 145 #################### */
    /* #################### ################################ #################### */

    /* Inter / Sans-serif */
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@100;200;300;400;500;600;700;800;900&display=swap');

    /* Global Styles */
    *,
    *::before,
    *::after {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      text-decoration: none;
    }

    html {
      font-size: 16px; /* Define font size */
      position: relative;
      overflow-x: hidden;
      scroll-behavior: smooth;
      font-family: 'Inter', sans-serif; /* Use Inter font family */
      background-color: #EDEEE9; /* Off-white background */
    }

    body {
      background-color: #EDEEE9; /* Off-white background */
      color: #303030; /* Black text color */
      min-height: 100vh;
      width: 100%;
      padding: 2.5vh 10vw;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: flex-start;
    }

    h1 {
      font-size: clamp(1rem, 6vw, 10rem); /* Responsive font size */
      margin: 15vh 0 2rem;
    }

    a {
      text-decoration: underline;
      color: #303030; /* Black color */
    }

    code {
      font-size: 1rem;
      padding: 0 0.5rem;
      background-color: #D8DBCE; /* Code background color */
    }

    .explanation {
      width: 100%;
      margin-bottom: 15vh;
    }

    .explanation p {
      font-size: 1rem;
      max-width: 75ch;
      margin: 2rem 0;
    }

    .explanation ul:last-child {
      margin-bottom: 2rem;
    }

    .explanation ul {
      display: flex;
      flex-direction: column;
    }

    .explanation ul li {
      list-style: none;
      position: relative;
      margin: 0.5rem 0;
    }

    .explanation ul li::before {
      content: '👉';
      position: absolute;
      top: -0.25rem;
      left: -2rem;
    }

    .container {
      width: 100%;
      max-width: 768px;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      padding: 2rem;
      background-color: #D8DBCE; /* Code background color */

      @media (min-width: 768px) {
        flex-direction: row;
        align-items: center;
      }
    }

    .cta-form {
      margin-bottom: 2rem;
      text-align: center;

      @media (min-width: 768px) {
        margin-bottom: 0;
        margin-right: 2rem;
        text-align: left;
      }
    }

    .cta-form h2 {
      font-size: 1.5rem;
      width: 100%;
      max-width: 25ch;
      margin: 0 auto;

      @media (min-width: 768px) {
        text-align: left;
        margin: 0;
      }
    }

    .cta-form p {
      margin-top: 1rem;
      font-size: 1rem;
      width: 100%;
      max-width: 24ch;
    }

    .form {
      align-self: center;
    }

    .form__input {
      width: clamp(120px, 50vw, 420px);
      height: 2.5rem;
      padding: 0 1.25rem;
      border: 1px solid #303030; /* Black border */
      border-radius: 2px;
      margin: 0.625rem auto;
      transition: all 250ms;

      @media (min-width: 768px) {
        width: clamp(120px, 35vw, 420px);
      }

      &:focus {
        outline: none;
        border-top-color: rgba(0, 0, 0, 0.1);
      }

      &:focus + .form__label, &:not(:placeholder-shown) + .form__label {
        transform: translateY(-4.50rem) scale(1);
        color: #303030; /* Black color */
      }

      &::placeholder {
        display: none;
        color: transparent;
        -webkit-user-select: none;
        -ms-user-select: none;
        user-select: none;
      }

      &:not(:placeholder-shown) {
        border-top-color: rgba(0, 0, 0, 0.5);
      }
    }

    .form__label {
      font-size: 1rem;
      color: #909090; /* Grey color */
      display: block;
      transform: translate(1.25rem,-2.5rem);
      transform-origin: 0 0;
      transition: all 500ms;
      -webkit-user-select: none;
      -ms-user-select: none;
      user-select: none;
      pointer-events: none; 
    }

    .alert {
      /* Add alert styles if needed */
    }
  </style>
</head>
<body>
  <div class="text-center mt-3">
<div class="container">
  <div class="cta-form">
    <h2>Envoyer un SMS</h2> 
  </div>
  <form method="post" action="{{ path('send_sms') }}" class="form">
    <div class="form-group">
      <input type="text" placeholder="Numéro de téléphone" class="form__input" id="number" name="number" required>
      <label for="number" class="form__label">Numéro de téléphone</label>
    </div>
    <div class="form-group">
      <input type="text" placeholder="Nom de l'expéditeur" class="form__input" id="name" name="name" required>
      <label for="name" class="form__label">Nom de l'expéditeur</label>
    </div>
    <div class="form-group">
      <textarea placeholder="Text" class="form__input" id="text" name="text" required></textarea>
      <label for="text" class="form__label">Text</label>
    </div>
    <button type="submit" class="btn btn-primary btn-block" id="btn">Envoyer le SMS</button>
  </form>
  {% if smsSent %}
    <div class="alert alert-success mt-3 text-center">Le SMS a été envoyé avec succès.</div>
  {% endif %}
</div></div>

<div class="explanation">
  <!-- Explanation content here -->
</div>

</body>
</html>
