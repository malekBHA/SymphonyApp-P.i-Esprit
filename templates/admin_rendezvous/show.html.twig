{% extends 'admin/base.html.twig' %}

{% block body %}
<main id="main" style="text-align: center;">
    <br><br><br><br><br> 
    <div style="background-color: #f0f0f0; padding: 20px; border-radius: 10px;">
        <h1 style="color: #333333; background-color: #ffffff; padding: 10px; border-radius: 5px;">RendezVous</h1>

        <table class="table" style="color: #333333; background-color: #ffffff; border-radius: 5px;">
            <tbody>
                <tr>
                    <th style="padding: 10px;">Id</th>
                    <td style="padding: 10px;">{{ rendez_vou.id }}</td>
                </tr>
                <tr>
                    <th style="padding: 10px;">Date</th>
                    <td style="padding: 10px;">{{ rendez_vou.date ? rendez_vou.date|date('Y-m-d') : '' }}</td>
                </tr>
               
               
            </tbody>
        </table>

        <a href="{{ path('Admin_rendez_index') }}" class="btn btn-primary" style=" color: #ffffff; border: none; border-radius: 5px; padding: 10px 20px; margin-right: 10px;">Back to List</a>

        <a href="{{ path('Admin_rendez_edit', {'id': rendez_vou.id}) }}" class="btn btn-primary" style="background-color: #ff9800; color: #ffffff; border: none; border-radius: 5px; padding: 10px 20px; margin-right: 10px;">Edit</a>
        {{ include('admin_rendezvous/_delete_form.html.twig') }}
    </div>
</main>
<br><br><br>
{% endblock %}
