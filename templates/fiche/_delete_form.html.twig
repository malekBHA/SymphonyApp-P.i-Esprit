<form method="post" action="{{ path('app_fiche_delete', {'id': fichepatient.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fichepatient.id) }}">
    <button class="btn btn-primary" style="position: relative; top: -47px; left: 1333px; background-color: red;">Delete</button>

</form>
