<form method="post" action="{{ path('app_react_delete', {'id': react.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ react.id) }}">
    <button class="btn">Delete</button>
</form>
